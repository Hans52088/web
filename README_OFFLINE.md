# ⌨️ 大易實體鍵盤轉接器 - iPad 離線完整版 (Dayi Bridge Offline)

這是一個專為 iPad（以及其他移動設備）設計的 **100% 離線型** 大易輸入法實體鍵盤轉接程式。

## 🌟 核心特色

- **完全離線**：所有字元資料（約 18,000+ 字）均直接內嵌於本地檔案，無需任何網路連接。
- **極速回應**：即按即顯，無網路延遲，提供最接近原生輸入法的流暢體驗。
- **隱私安全**：所有輸入過程均在您的瀏覽器本地處理，不經過任何伺服器。
- **iPad 優化**：完美匹配實體鍵盤的按鍵邏輯，特別適合使用鍵盤套的 iPad 用戶。
- **現代介面**：採用磨砂玻璃質感 (Glassmorphism) 設計，支援動態背景。

## 🛠️ 安裝與使用

1. **檔案下載**：
   - 確保下載了 `index.html` (或 `Dayi-ActualKB-transfer(Offline).html`)。
   - 確保同時下載了同目錄下的 `dayi_data.js`。
2. **開啟方式**：
   - 直接使用檔案管理 App (如 iOS 的「檔案」或 Documents) 開啟 `index.html`。
   - 使用 iOS 的 **Safari** 或主流瀏覽器瀏覽。
3. **輸入方式**：
   - 使用實體鍵盤進行大易字根輸入。
   - **選字按鍵**：`Space` (1), `'` (2), `[` (3), `]` (4), `-` (5), `\` (6)。
   - **功能按鍵**：`=` 進入標點模式，`Enter` 複製並清空。

## 📦 檔案結構

- `index.html`：主程式 UI 與邏輯。
- `dayi_data.js`：核心龐大字典檔（Rime 格式兼容）。

---
製作者：WHY | VERSION: 260413-1345
